machine:
  node:
    version: 8.6.0

general:
  branches:
    ignore:
      - master
      
test:
  override:
    - echo "skipping test"

deployment:
  staging:
    branch: source
    commands:
      - npm run deploy
